"""Boundary tests — verify refactored class interfaces and contracts.

Boundary tests ensure that key abstractions (repository protocols,
PluginRegistry, typed IDs) behave correctly at their interface
boundaries.
"""
